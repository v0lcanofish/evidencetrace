# EvidenceTrace · 设计 v7 —— 一个完整 agent + 它的行动决策算法

> 2026-09-17 ｜ v6 的问题：**堆了太多测评，算法本身很薄**。
> v7 把主体还给它该有的位置：**算法 + 完整链路**，测评压缩成验证手段。

---

## 0 · 一句话

> **一个药品咨询 agent。它自己决定每一步做什么——
> 查哪一节、要不要再查、要不要问用户、什么时候能答、什么时候该说不知道。**
>
> **而它的每一个决定都要过机械检查：引用能不能追溯到真实存在的证据块。**

---

## 1 · 定位：和项目一的分工

```
🅰️ 项目一 ToolHorizon
   训练一个 policy 学会多轮工具调用        ← 【训练侧】
   关键词：GRPO / 优势 / 稳定性 / 零方差
   它回答：模型在 RL 里会不会学坏

🅱️ 项目二 EvidenceTrace
   一个完整的药品咨询 agent + 它的决策算法   ← 【系统 + 算法侧】
   关键词：agent loop / 工具选择 / 停止准则 / 证据溯源
   它回答：agent 怎么用最少的动作拿到可追溯的答案
```

**⚠️ 不硬说成"同一件事"。它们是一个人的两条腿：一条会训模型，一条会造 agent。**

---

## 2 · ⭐ 算法核心：证据驱动的行动决策

**形式化**：

```
状态  s = (q, E, T, b)
        q 原始问题 ｜ E 已收集的证据（带契约）｜ T 已试过的动作 ｜ b 剩余预算

动作  a ∈ A = { locate, search, ask, answer, abstain }

策略  π(a | s)        ← ⭐ 这就是"算法"
```

**问题**：给定当前状态，下一个动作选什么，才能**用最少代价**拿到**可追溯的**答案？

**约束**：
- 每个动作有成本（token / 延迟 / 用户打扰）
- `answer` 的每条引用**必须**能追溯到契约（否则不许答）
- 证据覆盖不足时**必须**继续或拒答，不许硬答

---

## 3 · 策略的三档实现（**这是可对照的算法谱**）

| 档 | 做法 | 性质 |
|---|---|---|
| **① 规则** | `if 覆盖度 < 阈值: 继续 else: 停` | 启发式，零成本 |
| **② LLM** | 把状态描述给 LLM，让它选动作 | 有泛化，但不稳 |
| **③ 训练** ⭐ | 用 ①② 产生的决策日志 **SFT 一个小模型** | **靠上基模** |

**三档对照 = 论文式消融**：规则 vs LLM vs 训练。**而第 ③ 档需要训练。**

---

## 4 · 策略的具体算法点（五个）

| # | 算法点 | 要解决什么 |
|---|---|---|
| **①** | **问题分解** | 多跳问题怎么拆成子查询（"我有胃溃疡能吃布洛芬吗" → 查药 + 查禁忌 + 匹配） |
| **②** | **章节定位** | 意图 → LOINC 章节（**结构化先验**，不是语义猜） |
| **③** | **证据覆盖度** | ⭐ 怎么**算**出"证据够不够"（不是模型感觉） |
| **④** | **停止准则** | ⭐ 什么时候停 —— 太早幻觉，太晚浪费 |
| **⑤** | **冲突消解** | 多条证据矛盾时怎么办（不同说明书说法不同） |

**其中 ③④ 是核心**，它们决定了 agent 的行动边界。

### ③ 证据覆盖度怎么算

```
一个 claim 需要被支撑 → 找它需要的"证据槽位"
  "metformin 肾病患者能吃吗"
     槽位 = {药物, 禁忌条件, 关系}
     证据里有 <metformin, Renal disease, 禁忌> 吗？

coverage = 已填槽位 / 需要的槽位
```

**关键**：槽位是**从问题结构推出的**，填没填上是**查索引查出来的**——
**不是问 LLM"你觉得够了吗"。**

### ④ 停止准则

```
每轮结束算一次：
  coverage ≥ θ_high  →  停，去 answer
  coverage <  θ_low  →  继续（并给出"还缺哪个槽位"→ 直接变成下一次 search 的 query）
  中间地带           →  再查一轮，或 ask_user

⚠️ θ 不能拍脑袋 —— 要用评测集标定（这就是测评的用途，不是目的）
```

---

## 5 · 完整链路

```
┌──────────────────────────────────────────────────────────┐
│                    Agent Loop                             │
│                                                           │
│   q ──▶ ①问题分解 ──▶ 子问题列表                          │
│          ↓                                                │
│   ┌────────────────────────────────────────────┐          │
│   │  ②章节定位 → ③检索 → 收集证据（带契约）      │          │
│   │         ↓                                   │          │
│   │  ④算覆盖度 ──▶ 够了？ ──否──▶ 回到 ②③       │  ⭐循环  │
│   │                  │                         │          │
│   │                 是                         │          │
│   │                  ↓                         │          │
│   │  ⑤冲突消解 → 生成 claims（带契约引用）       │          │
│   │         ↓                                   │          │
│   │  ⑥机械核验：引用追得到吗？                    │          │
│   │         ↓ 追不到 → 回到 ②③                  │          │
│   └────────────────────────────────────────────┘          │
│                     ↓                                     │
│               answer() / abstain()                        │
│                                                           │
│   ⑦记忆：本轮学到什么 → 影响后续轮                          │
└──────────────────────────────────────────────────────────┘
```

**工具**：`locate` / `search` / `ask` / `answer` / `abstain`（agent 自选）
**状态**：已确认事实 / 待填槽位 / 已试查询 / 剩余预算
**反馈**：机械核验结果**反过来决定下一步**

---

## 6 · 测评（**压缩成验证手段，不是主体**）

**只留三样**：

| 用途 | 内容 | 规模 |
|---|---|---|
| **标定阈值** | 给 θ_low / θ_high 找依据 | ~60 条 |
| **验证策略** | 三档策略对照（规则 / LLM / 训练） | 同上，复用 |
| **边界** | 该拒答时拒不拒 | ~50 条 |

**合计 ~120 条**（v5 是 352 条，砍掉 2/3）。

**测评的存在意义只有一个**：证明算法有效、给阈值定依据。
**它本身不是贡献。**

---

## 7 · 实验结果长什么样（**这才是简历上的东西**）

| 策略 | 答案准确率 | 平均动作数 | 过度检索率 | 拒答准确率 |
|---|---|---|---|---|
| ① 规则阈值 | ? | ? | ? | ? |
| ② LLM 自评 | ? | ? | ? | ? |
| ③ 训练的决策器 | ? | ? | ? | ? |

**三行数字，讲的是"我们把行动决策从规则 → LLM → 训练，各提升多少"。**

**这才是算法项目的产出**，不是一堆评测表。

---

## 8 · 块划分（算法优先）

| 块 | 做什么 | 判据 |
|---|---|---|
| E1–E4 | 账本 / 引擎 / 语料 / 题 | ✅ |
| **E5** | **扩语料 + 精简评测集**（352 → ~120） | 自检绿 |
| **E6** | 检索层（BM25+定位+稠密+RRF+重排） | Recall@5 ≥ 0.90 |
| **E7** | ⭐ **Agent Loop + 工具集** | 能自主多轮，跑通端到端 |
| **E8** | ⭐ **覆盖度算法 + 停止准则**（策略①②） | 三档对照出数字 |
| **E9** | 生成 + 契约引用 + 机械核验 | 坏引用 100% 抓住 |
| **E10** | 记忆与状态 | 多轮一致 |
| **E11** | 冲突消解 | — |
| **E12** | ⭐ **训练决策器（策略③）** | **靠上基模，需 GPU** |
| **E13** | 跨层归因 | 错题指认层 |
| **E14** | 收口 | — |

---

## 9 · 语料

```
6 份药 → 50 份药（DailyMed API，零成本）
理由：评测不重复 + 泛化性 + 冲突消解需要多份说明书
```

---

## 10 · ⛔ 不做什么

```
多智能体对抗 ｜ 知识图谱 ｜ 多后端热切换 ｜ 通用深度研究
｜ 把控制流写死（退化成 RAG）
｜ 把精力堆在造评测集上（那是手段，不是目的）
```

---

## 11 · 诚实边界

1. 停止准则 / 覆盖度**不是全新概念**（Self-RAG 有反思机制），
   我们的增量是 **"用可追溯的证据覆盖度算，而不是让模型自省"**
2. `content_hash` 只证明出处真实，不证明推论正确
3. 语料 50 份药，非全库
4. 策略③训练用的是自己造的数据，有自造偏差

---

## 12 · 排期（~40h；③ 需要 GPU）

```
① 扩语料 6→50              0.5h
② 精简评测集（352→~120）    1.5h
③ E6 检索层补完            4h
④ E7 Agent Loop + 工具集    6h   ⭐
⑤ E8 覆盖度 + 停止准则      5h   ⭐ 核心算法
⑥ E9 生成 + 契约核验        4h
⑦ E10 记忆 / E11 冲突消解   5h
⑧ E13 归因                 3h
⑨ E12 训练决策器            5h   ⭐ 需要 GPU（轻量）
⑩ E14 收口                 6h
```
